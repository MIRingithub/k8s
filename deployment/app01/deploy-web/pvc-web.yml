apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app01-web            # penamaan pvc
  labels:
    app: app01-web           # penamaan label pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi